# Simple music player
A webpage to share my music on, for my artproject [3.Ndangered](https://egnrse.eu/music).




## Usage
You need `typescript` (`tsc`) and `php` installed on your system. (Install ts with eg. `npm install -g typescript`.)

You can compile this project by running `tsc` in the root folder of this repo.  
You can test it with eg. `php -S localhost:8000`. You should now see it on [http://localhost:8000](http://localhost:8000).



### Notes
`tsc --watch` (autocompile on changes)  
`php -d display_errors=1 files.php` (show php errors in a file)  

## License
I am unsure what i want to do with this in the future. But for now you are free to use/change this yourself if you:
  - keep it and all changes open-source (see [GPL-3](https://www.gnu.org/licenses/gpl-3.0.en.html))
  - always attribute me (or this repo) (similar to CC-BY)
